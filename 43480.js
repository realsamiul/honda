// This file was generated by Slater.app - parallax.js


// function parallax() {
//   let mm = gsap.matchMedia();
//   mm.add("(min-width: 768px)", () => {
//     gsap.utils.toArray("[data-scroll-speed]").forEach((el) => {
//       const speed = parseFloat(el.dataset.scrollSpeed);

//       ScrollTrigger.create({
//         trigger: el,
//         start: "top bottom", // when the top of the element hits the bottom of the viewport
//         end: "bottom top", // when the bottom of the element hits the top of the viewport
//         scrub: true,
//         // markers: true,
//         onUpdate: (self) => {
//           const progress = self.progress;
//           const distance = window.innerHeight + el.offsetHeight;
//           const y = (progress - 0.065) * distance * -speed;
//           gsap.set(el, { y });
//         }
//       });
//     });
//   });
// }
// parallax();

// function xparallax() {
//   let mm = gsap.matchMedia();
//   mm.add("(min-width: 768px)", () => {
//     const container = document.querySelector(".analog");
//     const scrollWidth = container.scrollWidth - window.innerWidth;

//     gsap.utils.toArray("[data-scroll-speed]").forEach((el) => {
//       const speed = parseFloat(el.dataset.scrollSpeed);

//       ScrollTrigger.create({
//         trigger: container,
//         start: "top top",
//         end: () => '+=20%',
//         scrub: true,
//         // markers: true,
//         onUpdate: (self) => {
//           const progress = self.progress;
//           const x = progress * scrollWidth * speed * -1;
//           gsap.set(el, { x });
//         }
//       });
//     });
//   });
// }

// xparallax();


